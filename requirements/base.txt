# ============================================================
# base.txt — Shared across ALL services (api, workers, cron)
# Anything imported from src/shared/ lives here.
# ============================================================

# Core framework utilities (pydantic-settings used in config.py)
pydantic==2.12.5
pydantic-settings==2.12.0
pydantic_core==2.41.5
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0

# Database (SQLAlchemy + psycopg2 used in src/shared/core/database.py)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.11
psycopg==3.2.3

# Azure Storage SDK (queue + blob used in src/shared/services/)
azure-storage-blob==12.19.0
azure-storage-queue==12.9.0
azure-core==1.36.0
isodate==0.7.2

# HTTP client (used in shared services + handlers)
requests==2.32.5
httpx==0.28.1
httpcore==1.0.9
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.0

# Env / dotenv
python-dotenv==1.2.1

# Logging / stdlib helpers
sniffio==1.3.1
anyio==4.12.0
